name: Publish Docker Image

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write   # 建立 GitHub Release 需要

jobs:
  push:
    name: Build & push to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: syhwanlin/xxl-job-exporter
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Docker Hub 的 overview 同步成 README.en.md，short description 一併更新。
      # 需要 DOCKERHUB_TOKEN 具備 Read & Write 權限。
      - name: Update Docker Hub description
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: syhwanlin/xxl-job-exporter
          readme-filepath: ./README.en.md
          short-description: "Non-invasive Prometheus exporter for XXL-JOB (reads MySQL/MariaDB directly, read-only)."

      # 依 tag 自動建立 GitHub Release，release notes 由 PR / commit 自動產生
      # （分類規則見 .github/release.yml）。
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
